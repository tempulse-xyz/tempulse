# Tempulse

This is a Rust monorepo workspace for the Tempulse project.

## Structure

- `crates/core`: Core library (`tempulse-core`)

## Development

To build the project:

```bash
cargo build
```

To run tests:

```bash
cargo test
```
